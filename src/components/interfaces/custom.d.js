// SVG Module Declaration
// This allows importing SVG files as React components
if (typeof module !== 'undefined' && module.hot) {
    // Module hot replacement support for development
}

export default {};
